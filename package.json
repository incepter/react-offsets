{
  "name": "react-offsets",
  "author": "incepter",
  "license": "MIT",
  "repository": "incepter/react-offsets ",
  "main": "dist/index.js",
  "version": "0.1.2",
  "private": false,
  "peerDependencies": {
    "react": "^16.8.6"
  },
  "devDependencies": {
    "rimraf": "2.6.3",
    "@babel/cli": "^7.7.5",
    "@babel/plugin-proposal-optional-chaining": "^7.7.5",
    "@babel/preset-react": "^7.7.4"
  },
  "babel": {
    "presets": [
      "@babel/preset-react"
    ],
    "plugins": [
      "@babel/plugin-proposal-optional-chaining"
    ]
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "publish:npm": "rimraf ./dist && mkdir dist &&  babel ./src/index.js -d dist --copy-files"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
